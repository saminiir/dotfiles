alias startx='startx &> ~/.xlog'

# Custom aliases

alias t='tig'
alias ts='tig status'
alias h='history'
alias m='man'

# Custom functions

function f() {
    find . -iname "*$1*" 2>/dev/null 
}

function sleep_now() {
    slock &
    xset dpms force suspend
}
